---
// eslint-disable-next-line @typescript-eslint/consistent-type-definitions
export interface Props {
  class: string
  title?: string
}

const { class: className, ...attrs } = Astro.props
---

<button class:list={['btn', className]} {...attrs} >
  <slot />
</button>
